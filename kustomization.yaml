apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - namespaces.yaml
  - flux-system
  - secrets
  - apps

patches:
  - path: flux-patch.yaml
    target:
      kind: Kustomization
      name: flux-system
